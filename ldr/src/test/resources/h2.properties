# DB_CLOSE_DELAY=-1 keeps the in-memory database alive after the last
# connection is closed - Loader.close() closes its connection, so without
# this the schema and all inserted rows would be gone before verification.
url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1
